/* ---------------------------------------------------------
   Hero — Primary landing section
   Future: Three.js canvas background, animated text,
   scroll-triggered reveal, CTA buttons
   --------------------------------------------------------- */

export default function Hero() {
  return (
    <section id="hero">
      {/* 
        Planned elements:
        - Three.js WebGL canvas (background)
        - Animated headline + subheadline
        - Primary & secondary CTA buttons
        - Scroll indicator
      */}
    </section>
  );
}
