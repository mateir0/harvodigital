/* ---------------------------------------------------------
   Portfolio — Showcases agency work / case studies
   Future: filterable grid, hover previews, lightbox,
   animated transitions between items
   --------------------------------------------------------- */

export default function Portfolio() {
  return (
    <section id="portfolio">
      {/* 
        Planned elements:
        - Section heading
        - Filter/category tabs
        - Project cards with hover overlays
        - Links to individual case study pages
      */}
    </section>
  );
}
