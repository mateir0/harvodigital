/* ---------------------------------------------------------
   Services — Showcases agency services/capabilities
   Future: animated service cards, hover effects, icons
   --------------------------------------------------------- */

export default function Services() {
  return (
    <section id="services">
      {/* 
        Planned elements:
        - Section heading
        - Service cards grid (design, development, branding, etc.)
        - Animated icons per service
        - CTA link per service
      */}
    </section>
  );
}
